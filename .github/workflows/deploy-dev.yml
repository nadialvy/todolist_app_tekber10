name: Deploy to Dev

on:
  push:
    branches: [dev]

env:
  FLUTTER_VERSION: "3.38.3"

jobs:
  deploy:
    name: Build & deploy to dev
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      pull-requests: read

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Create .env
        run: echo "OPENAI_API_KEY=${{ secrets.DEV_OPENAI_API_KEY }}" > .env

      - name: Build Flutter Web
        run: flutter build web --release --dart-define=ENVIRONMENT=dev

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_SA_EMAIL }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Backup current dev deployment
        run: |
          TIMESTAMP=$(date +%Y%m%d%H%M%S)
          echo "RELEASE_TIMESTAMP=${TIMESTAMP}" >> "$GITHUB_ENV"
          gsutil -m rsync -r \
            -x "^releases/.*$|^pre-rollbacks/.*$" \
            gs://${{ secrets.GCP_DEV_BUCKET }} \
            gs://${{ secrets.GCP_DEV_BUCKET }}/releases/${TIMESTAMP} \
          || echo "No existing deployment to backup — skipping"

      - name: Deploy to dev
        run: |
          gsutil -m rsync -r -d \
            -x "^releases/.*$|^pre-rollbacks/.*$" \
            build/web \
            gs://${{ secrets.GCP_DEV_BUCKET }}

      - name: Write release manifest
        run: |
          cat <<EOF | gsutil cp - gs://${{ secrets.GCP_DEV_BUCKET }}/releases/${{ env.RELEASE_TIMESTAMP }}/manifest.json
          {
            "environment": "dev",
            "sha": "${{ github.sha }}",
            "ref": "${{ github.ref_name }}",
            "actor": "${{ github.actor }}",
            "timestamp": "${{ env.RELEASE_TIMESTAMP }}",
            "run_id": "${{ github.run_id }}"
          }
          EOF

      - name: Get merged PR assignees
        id: assignees
        if: always()
        continue-on-error: true
        uses: actions/github-script@v7
        env:
          NOTIFY_EMAILS: ${{ secrets.NOTIFY_EMAILS }}
<<<<<<< Updated upstream
          USER_EMAIL_MAP: ${{ secrets.USER_EMAIL_MAP }}
=======
>>>>>>> Stashed changes
        with:
          script: |
            const { data: prs } = await github.rest.repos.listPullRequestsAssociatedWithCommit({
              owner: context.repo.owner,
              repo: context.repo.repo,
              commit_sha: context.sha,
            });
            const logins = prs.flatMap(pr => pr.assignees.map(a => a.login));
<<<<<<< Updated upstream
            const emailMap = Object.fromEntries(
              (process.env.USER_EMAIL_MAP || '').split(',')
                .map(e => e.trim().split(':'))
                .filter(parts => parts.length === 2)
            );
            const emails = logins.map(login => emailMap[login]).filter(Boolean);
=======
            const emails = [];
            for (const login of logins) {
              const { data: user } = await github.rest.users.getByUsername({ username: login });
              if (user.email) emails.push(user.email);
            }
>>>>>>> Stashed changes
            const fallback = process.env.NOTIFY_EMAILS;
            core.setOutput('logins', logins.length > 0 ? logins.join(', ') : context.actor);
            core.setOutput('emails', emails.length > 0 ? emails.join(',') : fallback);

      - name: Send success notification
        if: success()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          secure: false
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "[Dev] Deployment succeeded — ${{ github.repository }}"
          to: ${{ steps.assignees.outputs.emails || secrets.NOTIFY_EMAILS }}
          from: "${{ secrets.SMTP_USERNAME }}"
          body: |
            Dev deployment succeeded.

            Repository : ${{ github.repository }}
            Branch     : ${{ github.ref_name }}
            Commit     : ${{ github.sha }}
            Triggered  : ${{ github.actor }}
            Assignees  : ${{ steps.assignees.outputs.logins }}
<<<<<<< Updated upstream
            URL        : https://storage.googleapis.com/${{ secrets.GCP_DEV_BUCKET }}/current/index.html
=======
            URL        : https://storage.googleapis.com/${{ secrets.GCP_DEV_BUCKET }}/index.html
>>>>>>> Stashed changes

            View run   : ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

      - name: Send failure notification
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          secure: false
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "[Dev] Deployment failed — ${{ github.repository }}"
          to: ${{ steps.assignees.outputs.emails || secrets.NOTIFY_EMAILS }}
          from: "${{ secrets.SMTP_USERNAME }}"
          body: |
            Dev deployment failed.

            Repository : ${{ github.repository }}
            Branch     : ${{ github.ref_name }}
            Commit     : ${{ github.sha }}
            Triggered  : ${{ github.actor }}
            Assignees  : ${{ steps.assignees.outputs.logins }}

            View run   : ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
